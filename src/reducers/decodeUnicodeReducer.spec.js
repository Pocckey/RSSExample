import expect from 'expect';
import feedDecodeReducer from './decodeUnicodeReducer';
import * as decodeActions from '../actions/decodeUnicodeAction';
import initialState from './initialState';

describe('unicode decode Redcuer', () =>{
  it('should decode unicode', () =>{
    expect(true).toEqual(true);
  });
});
